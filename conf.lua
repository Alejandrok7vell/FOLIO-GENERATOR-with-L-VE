function love.conf(t)
    t.console = false
    t.window.title = "Creador de Folios - Professional Plus Business Enterprise Edition 2024 uwu"         -- The window title (string)
    t.window.icon = "logo.png"                 -- Filepath to an image to use as the window's icon (string)
    t.window.width = 700             -- The window width (number)
    t.window.height = 368               -- The window height (number)
    t.window.borderless = false         -- Remove all border visuals from the window (boolean)
    t.window.resizable = false          -- Let the window be user-resizable (boolean)
end